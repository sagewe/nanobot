# nanobot-rs Builtin Skills

This directory contains builtin skills for the Rust runtime.

Each skill lives in its own subdirectory and must include `SKILL.md`.
Workspace skills at `<workspace>/skills/<name>/SKILL.md` override builtin
skills with the same normalized name.
